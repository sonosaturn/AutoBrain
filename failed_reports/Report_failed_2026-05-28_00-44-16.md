# 🤖 REPAIR Report - 2026-05-28_00-44-16

## 💡 Proposal
As Senior Debugger, I have evaluated the proposal for **Graph-Weighted Context Reranking (GWCR)**. This is a sound architectural improvement that addresses the lack of structural context in the current vector-only retrieval system.

Below is the technical implementation to resolve this. We will modify `autobrain_core/graph_manager.py` to integrate frequency-based scoring and implement the weighted formula.

### Technical Implementation

#### 1. Update `autobrain_core/graph_manager.py`
We will modify `get_context_for_query` to incorporate the `get_wikilink_frequencies` logic and apply the reranking formula.

```python
import math
from typing import List, Dict, Any
# Existing imports...
from autobrain_core.materialize_nodes import get_wikilink_frequencies

def get_context_for_query(query: str, limit: int = 10, weights: Dict[str, float] = None) -> List[Dict[str, Any]]:
    """
    Retrieves context using BM25 and Vector search, then reranks based on 
    graph centrality (link frequency).
    """
    if weights is None:
        weights = {"similarity": 0.7, "graph": 0.3}

    # 1. Fetch raw candidates from BM25 and Vector Search
    # This represents the existing retrieval logic
    candidates = _perform_hybrid_search(query, search_limit=limit * 2) 
    
    # 2. Extract Graph Metadata
    # We get the frequency map (how many times each note is linked)
    freq_map = get_wikilink_frequencies()
    
    reranked_results = []
    
    for item in candidates:
        node_id = item.get("id")
        sim_score = item.get("score", 0.0)
        
        # Get frequency for this node
        link_count = freq_map.get(node_id, 0)
        
        # 3. Apply Ghost Link Penalty
        # If a node has 0 links, it's often a fragment or a 'ghost'
        if link_count == 0:
            sim_score *= 0.8  # 20% penalty for isolated nodes
            
        # 4. Calculate Final Score using the heuristic:
        # FinalScore = (SimScore * Ws) + (log(Freq + 1) * Wg)
        graph_boost = math.log(link_count + 1)
        final_score = (sim_score * weights["similarity"]) + (graph_boost * weights["graph"])
        
        item["rerank_score"] = final_score
        reranked_results.append(item)
        
    # 5. Sort by the new weighted score and truncate to requested limit
    reranked_results.sort(key=lambda x: x["rerank_score"], reverse=True)
    
    return reranked_results[:limit]

def _perform_hybrid_search(query: str, search_limit: int):
    """
    Internal helper wrapping the existing BM25 + Vector logic 
    currently found in lines 49-50.
    """
    # ... (Existing vector/bm25 merging logic) ...
    pass
```

### Why this resolves the error:
1.  **Reduces Latency Impact:** While it doesn't speed up the vector search itself, it ensures that the high-latency cost (794.9ms) results in significantly higher-quality context, reducing the need for "multi-turn" clarifications.
2.  **Structural Intelligence:** By using `math.log(link_count + 1)`, we give a diminishing returns boost to highly linked notes. A note with 10 links is much more important than one with 0, but a note with 100 links is only slightly more important than one with 80.
3.  **Noise Reduction:** The penalty for nodes with 0 links (Ghost Links) prevents the AI from hallucinating based on incomplete "leaf" notes or stray wikilinks.

### Recommendation for Lorenzo:
Once this is applied, run the following to verify the improvement in context quality:
`gitnexus analyze --check-context "your query"` 

**Status:** `Resolution Proposed`
**Action:** Update `autobrain_core/graph_manager.py` with the provided logic.
---
## 🛠️ Result
🛰️ Task delegated to Gemini CLI. File ready at C:/Users/Lorenzo/Documents/conversation_vault\Jarvis\_Dispatch_Queue\TASK_DELEGATION_2026-05-28_00-44-16.md. Inform Lorenzo.